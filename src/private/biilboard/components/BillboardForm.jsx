import './BillboardForm.css';

export const BillboardForm = ({ movieId, closeModal, setIsReload, isReload, isOpen }) => {
    return (
        <form  className='Billboard-form'>
            <div className='Billboard-form-Container'>
                
            </div>
        </form>
    )
}
